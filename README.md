# pi-pou
pi-pou is a discord bot i made

pi-pou can do things like:
* talk

## `.env` Variables
| Name | Type |
| -----| ---- |
| `CLIENT_ID` | number |
| `DISCORD_TOKEN` | string |
| `OPENAI_TOKEN` | string |